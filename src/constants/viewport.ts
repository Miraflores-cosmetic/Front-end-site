/**
 * Единые пороги для `useScreenMatch(max)` (true, если `window.innerWidth < max`).
 * Держим в одном месте, чтобы хедер, футер и страницы переключались синхронно.
 */
export const VIEWPORT_MOBILE_MAX = 768;

export const VIEWPORT_TABLET_MAX = 1024;
