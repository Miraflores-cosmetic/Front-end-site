# TODO: Улучшения процесса регистрации

## 🔴 ВЫСОКИЙ ПРИОРИТЕТ

### 1. Валидация email перед отправкой формы

**Файл:** `Front/src/pages/SignUp/LazyComponent/index.tsx`

**Задачи:**
- [ ] Создать функцию валидации email: `validateEmail(email: string): boolean`
  - Использовать regex: `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`
  - Проверять на пустоту и формат
- [ ] Добавить состояние для ошибок валидации: `emailError: string | null`
- [ ] Валидировать email при изменении (onChange) или при потере фокуса (onBlur)
- [ ] Показывать ошибку под полем email, если формат неверный
- [ ] Блокировать отправку формы, если email невалиден
- [ ] Добавить визуальный индикатор (красная рамка) для невалидного email

**Пример реализации:**
```typescript
const [emailError, setEmailError] = useState<string | null>(null);

const validateEmail = (email: string): boolean => {
  const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
  return emailRegex.test(email.trim());
};

const handleEmailChange = (e: React.ChangeEvent<HTMLInputElement>) => {
  const value = e.target.value;
  dispatch(setEmail(value));
  
  if (value && !validateEmail(value)) {
    setEmailError('Введите корректный email адрес');
  } else {
    setEmailError(null);
  }
};
```

---

### 2. Индикатор загрузки при регистрации

**Файл:** `Front/src/pages/SignUp/LazyComponent/index.tsx`

**Задачи:**
- [ ] Использовать состояние `signUp.loadingStatus` из Redux для отображения загрузки
- [ ] Изменить текст кнопки "Далее" на "Регистрация..." во время загрузки
- [ ] Блокировать кнопку (disabled) во время загрузки
- [ ] Добавить визуальный индикатор (спиннер) на кнопке или рядом с формой
- [ ] Предотвратить повторные клики по кнопке во время обработки

**Пример реализации:**
```typescript
<Button
  text={signUp.loadingStatus ? 'Регистрация...' : 'Далее'}
  onClick={() => dispatch(sendSignUpData({ email, pass }))}
  disabled={!signUp.agreeChecked || signUp.loadingStatus || !!emailError}
/>
```

---

### 3. Сохранение email в Redux state вместо только localStorage

**Файлы:** 
- `Front/src/store/slices/authSlice.ts`
- `Front/src/pages/SignUp/LazyComponent/index.tsx`
- `Front/src/pages/EmailConfirmation/EmailConfirmation.tsx`

**Задачи:**
- [ ] Убедиться, что email сохраняется в Redux state при регистрации
- [ ] В `authSlice.ts` в `sendSignUpData.fulfilled` сохранять email в state (уже есть в строке 130)
- [ ] В `EmailConfirmation.tsx` получать email из Redux state, а не только из localStorage
- [ ] Добавить fallback: если email нет в Redux, брать из localStorage
- [ ] Очищать email из localStorage после успешного подтверждения

**Пример реализации:**
```typescript
// В EmailConfirmation.tsx
const { email: emailFromState } = useSelector((state: RootState) => state.authSlice);
const email = emailFromState || localStorage.getItem('email') || '';
```

---

### 4. Показ требований к паролю перед вводом

**Файл:** `Front/src/pages/SignUp/LazyComponent/index.tsx`

**Задачи:**
- [ ] Создать компонент или секцию с требованиями к паролю
- [ ] Показывать требования под полем пароля (всегда видимы или при фокусе)
- [ ] Визуально отмечать выполненные требования (галочки/цвет)
- [ ] Требования:
  - Минимум 8 символов
  - Рекомендуется: заглавная буква, строчная буква, цифра
- [ ] Добавить стили для отображения требований

**Пример реализации:**
```typescript
const passwordRequirements = [
  { text: 'Минимум 8 символов', met: pass.length >= 8 },
  { text: 'Заглавная буква', met: /[A-Z]/.test(pass) },
  { text: 'Строчная буква', met: /[a-z]/.test(pass) },
  { text: 'Цифра', met: /[0-9]/.test(pass) },
];

// В JSX под полем пароля
<div className={styles.passwordRequirements}>
  {passwordRequirements.map((req, idx) => (
    <p key={idx} className={req.met ? styles.met : styles.unmet}>
      {req.met ? '✓' : '○'} {req.text}
    </p>
  ))}
</div>
```

---

## 🟡 СРЕДНИЙ ПРИОРИТЕТ

### 5. Улучшение обработки ошибок

**Файлы:**
- `Front/src/pages/SignUp/LazyComponent/index.tsx`
- `Front/src/utils/translateAuthError.ts`
- `Front/src/graphql/queries/auth.service.ts`

**Задачи:**
- [ ] Добавить обработку сетевых ошибок (network error, timeout)
- [ ] Добавить обработку ошибок GraphQL (проверка result.errors)
- [ ] Расширить `translateAuthError.ts` с дополнительными ошибками:
  - "Network error" → "Проблема с подключением. Проверьте интернет"
  - "Timeout" → "Превышено время ожидания. Попробуйте позже"
  - "Invalid email format" → "Неверный формат email"
  - "Password too weak" → "Пароль слишком простой"
- [ ] Добавить логирование ошибок в консоль для отладки (только в dev режиме)
- [ ] Показывать понятные сообщения пользователю вместо технических ошибок

**Пример реализации:**
```typescript
// В translateAuthError.ts
const AUTH_ERROR_MAP: Record<string, string> = {
  // ... существующие
  'Network request failed': 'Проблема с подключением. Проверьте интернет',
  'Failed to fetch': 'Не удалось подключиться к серверу',
  'Timeout': 'Превышено время ожидания. Попробуйте позже',
  'Invalid email format': 'Неверный формат email адреса',
};

// В auth.service.ts
try {
  const result = await graphqlRequest<SignUpMutationResponse>(mutation, variables);
  // ...
} catch (error: any) {
  if (error.message.includes('fetch') || error.message.includes('network')) {
    throw new Error('Проблема с подключением. Проверьте интернет');
  }
  throw error;
}
```

---

### 6. Унификация процесса подтверждения email

**Файл:** `Front/src/pages/EmailConfirmation/EmailConfirmation.tsx`

**Задачи:**
- [ ] Определить приоритетный способ подтверждения (рекомендуется код через REST API)
- [ ] Если используется код, убрать или упростить логику с токеном из URL
- [ ] Если используется токен, убрать поле ввода кода
- [ ] Добавить четкие инструкции пользователю о том, что делать
- [ ] Улучшить сообщения на странице подтверждения
- [ ] Добавить информацию о том, куда было отправлено письмо (показывать маскированный email)

**Пример реализации:**
```typescript
// Показывать маскированный email
const maskEmail = (email: string): string => {
  const [local, domain] = email.split('@');
  if (local.length <= 2) return email;
  return `${local[0]}${'*'.repeat(local.length - 2)}${local[local.length - 1]}@${domain}`;
};

// В JSX
<p className={styles.desc}>
  Мы отправили код подтверждения на {maskEmail(email)}
</p>
```

---

### 7. Добавление retry логики для неудачных запросов

**Файл:** `Front/src/graphql/queries/auth.service.ts`

**Задачи:**
- [ ] Создать утилиту `retryRequest` для повторных попыток
- [ ] Применять retry только для сетевых ошибок (не для ошибок валидации)
- [ ] Максимум 3 попытки с экспоненциальной задержкой (1s, 2s, 4s)
- [ ] Показывать пользователю, что идет повторная попытка (опционально)
- [ ] Применять retry для:
  - `signUpService`
  - `getToken`
  - `confirmEmailRequest`
  - `verifyEmailCode`

**Пример реализации:**
```typescript
const retryRequest = async <T>(
  fn: () => Promise<T>,
  retries = 3,
  delay = 1000
): Promise<T> => {
  for (let i = 0; i < retries; i++) {
    try {
      return await fn();
    } catch (error: any) {
      const isNetworkError = error?.message?.includes('fetch') || 
                            error?.message?.includes('network') ||
                            error?.message?.includes('Failed to fetch');
      
      if (!isNetworkError || i === retries - 1) {
        throw error;
      }
      
      // Экспоненциальная задержка
      await new Promise(resolve => setTimeout(resolve, delay * Math.pow(2, i)));
    }
  }
  throw new Error('Max retries exceeded');
};

// Использование
export async function signUpService(email: string, password: string) {
  return retryRequest(async () => {
    const result = await graphqlRequest<SignUpMutationResponse>(mutation, variables);
    // ... обработка результата
    return result.accountRegister.user;
  });
}
```

---

### 8. Улучшение UX сообщений и обратной связи

**Файлы:**
- `Front/src/pages/SignUp/LazyComponent/index.tsx`
- `Front/src/pages/EmailConfirmation/EmailConfirmation.tsx`

**Задачи:**
- [ ] Добавить информационные сообщения (не только ошибки)
- [ ] Показывать прогресс регистрации (шаг 1 из 2: Регистрация → Подтверждение)
- [ ] Улучшить сообщения на странице подтверждения email
- [ ] Добавить подсказки под полями (placeholder или helper text)
- [ ] Показывать успешные действия (например, "Письмо отправлено")

**Пример реализации:**
```typescript
// В SignUp
<div className={styles.progressBar}>
  <div className={styles.step} data-active={true}>
    <span>1</span> Регистрация
  </div>
  <div className={styles.step}>
    <span>2</span> Подтверждение email
  </div>
</div>

// В EmailConfirmation
<div className={styles.infoBox}>
  <p>✓ Письмо отправлено на {maskEmail(email)}</p>
  <p>Проверьте папку "Спам", если письмо не пришло</p>
</div>
```

---

### 9. Валидация пароля перед отправкой формы

**Файл:** `Front/src/pages/SignUp/LazyComponent/index.tsx`

**Задачи:**
- [ ] Создать функцию валидации пароля: `validatePassword(password: string): { valid: boolean; errors: string[] }`
- [ ] Проверять минимальную длину (8 символов)
- [ ] Опционально: проверять наличие заглавной, строчной буквы, цифры
- [ ] Показывать ошибки валидации под полем пароля
- [ ] Блокировать отправку формы, если пароль невалиден
- [ ] Синхронизировать с требованиями к паролю (задача #4)

**Пример реализации:**
```typescript
const validatePassword = (password: string): { valid: boolean; errors: string[] } => {
  const errors: string[] = [];
  if (password.length < 8) {
    errors.push('Минимум 8 символов');
  }
  // Опциональные проверки
  if (password.length > 0 && !/[A-Z]/.test(password)) {
    errors.push('Рекомендуется добавить заглавную букву');
  }
  if (password.length > 0 && !/[0-9]/.test(password)) {
    errors.push('Рекомендуется добавить цифру');
  }
  return { valid: errors.length === 0, errors };
};
```

---

### 10. Обработка edge cases

**Файлы:**
- `Front/src/pages/SignUp/LazyComponent/index.tsx`
- `Front/src/pages/EmailConfirmation/EmailConfirmation.tsx`

**Задачи:**
- [ ] Обработка случая, когда пользователь уже зарегистрирован (показывать ссылку на вход)
- [ ] Обработка случая, когда email уже подтвержден (редирект на главную)
- [ ] Обработка истекшего кода подтверждения (позволить запросить новый)
- [ ] Обработка случая, когда токен подтверждения невалиден (показать ошибку и предложить повторную отправку)
- [ ] Обработка случая, когда пользователь закрыл страницу во время регистрации

**Пример реализации:**
```typescript
// В EmailConfirmation
useEffect(() => {
  // Проверка, не подтвержден ли уже email
  const checkEmailConfirmed = async () => {
    try {
      const me = await getMeInfo();
      if (me?.isConfirmed) {
        toast.info('Ваш email уже подтвержден');
        navigate('/');
      }
    } catch (error) {
      // Игнорируем ошибку, если пользователь не авторизован
    }
  };
  checkEmailConfirmed();
}, []);
```

---

## 📝 Примечания

- Все изменения должны быть протестированы на разных браузерах
- Убедиться, что мобильная версия работает корректно
- Проверить доступность (accessibility) для всех новых элементов
- Обновить типы TypeScript при необходимости
- Добавить комментарии к сложной логике

## 🎯 Порядок выполнения

1. **Сначала высокий приоритет:**
   - Валидация email (#1)
   - Индикатор загрузки (#2)
   - Сохранение email в Redux (#3)
   - Требования к паролю (#4)

2. **Затем средний приоритет:**
   - Обработка ошибок (#5)
   - Унификация подтверждения (#6)
   - Retry логика (#7)
   - UX улучшения (#8)
   - Валидация пароля (#9)
   - Edge cases (#10)

## ✅ Критерии готовности

Каждая задача считается выполненной, когда:
- Код написан и протестирован
- Нет ошибок линтера
- Работает на desktop и mobile
- Обработаны все edge cases
- Добавлены необходимые типы TypeScript
